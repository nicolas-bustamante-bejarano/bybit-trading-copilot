"use client";
/* eslint-disable react-hooks/set-state-in-effect */
import { useCallback, useEffect, useState } from "react";
import { api } from "@/lib/api/client";
import type { Coach, Sizing, TradePlan } from "@/lib/api/types";
import { usePolling } from "@/lib/api/use-polling";
import { PlanEditor } from "./workspace/plan-editor";
import { PriceChart } from "./workspace/price-chart";
import { StructureEditor } from "./workspace/structure-editor";
import { PageHeader, Panel } from "./ui";
const symbols=["BTCUSDT","ETHUSDT","BNBUSDT","SOLUSDT","BONKUSDT"],frames=["5m","15m","1h","4h","1D","3D"];
export function WorkspaceClient(){const [symbol,setSymbol]=useState("BTCUSDT"),[frame,setFrame]=useState("1h"),[plans,setPlans]=useState<TradePlan[]>([]),[plan,setPlan]=useState<TradePlan|null>(null),[coach,setCoach]=useState<Coach|null>(null),[sizing,setSizing]=useState<Sizing|null>(null);const loadChart=useCallback(()=>api.getChart(symbol,frame),[symbol,frame]),loadStructures=useCallback(()=>api.getChartStructures(symbol),[symbol]),loadChanges=useCallback(()=>api.getStateChanges(50),[]);const {data:chart}=usePolling(loadChart,5000),{data:structures,refresh:refreshStructures}=usePolling(loadStructures,15000),{data:changes}=usePolling(loadChanges,15000);const refreshPlans=async()=>setPlans(await api.getTradePlans());useEffect(()=>{void refreshPlans();void api.getPositionCoach(symbol).then(setCoach).catch(()=>setCoach(null));},[symbol]);const relevant=plans.filter(x=>x.symbol===symbol),active=relevant.filter(x=>x.lifecycle_status==="ACTIVE");const size=async()=>{if(plan)setSizing(await api.getSizing({symbol,side:plan.side,entry:(plan.entry_probe_plan as Record<string,string>).entry,hard_invalidation:plan.hard_invalidation,max_risk_percent:plan.max_risk_percent,correlation_group:plan.correlation_group,trade_plan_id:plan.id}))};const permission=!coach?"UNAVAILABLE":coach.execution.state==="ADD"&&coach.execution.add_allowed?"ADD ALLOWED":coach.execution.state==="HOLD"?"HOLD · ADD LOCKED":coach.execution.state;return <><PageHeader eyebrow="LIVE TRADING WORKSPACE" title="Chart, plan, and execution discipline" detail="Read-only analysis. Every exchange action remains manual on Bybit."/><div className="mb-4 flex flex-wrap gap-2">{symbols.map(x=><button key={x} onClick={()=>{setSymbol(x);setPlan(null)}} className={x===symbol?"rounded bg-cyan-400/15 px-3 py-2 text-xs text-cyan-200":"rounded bg-white/5 px-3 py-2 text-xs"}>{x}</button>)}{frames.map(x=><button key={x} onClick={()=>setFrame(x)} className="px-2 text-xs text-slate-400">{x}</button>)}</div><div className="grid gap-4 2xl:grid-cols-[minmax(0,1fr)_390px]"><div className="space-y-4"><Panel title={`${symbol} · ${frame}`} kicker="TEMPORARY 5s MARKET REFRESH"><PriceChart chart={chart} plan={plan} structures={structures??[]} changes={changes??[]}/></Panel><StructureEditor symbol={symbol} timeframe={frame} rows={structures??[]} refresh={refreshStructures}/></div><aside className="space-y-4"><Panel title="Plan selection" kicker={active.length>1?"AMBIGUOUS":"PERSISTED INTENT"}>{active.length>1&&<p className="mb-2 text-xs text-rose-300">Multiple active plans: sizing remains safely blocked.</p>}<select value={plan?.id??""} onChange={e=>setPlan(relevant.find(x=>x.id===e.target.value)??null)} className="w-full bg-white/5 p-2 text-xs"><option value="">Select plan</option>{relevant.map(x=><option key={x.id} value={x.id}>{x.lifecycle_status} · {x.setup_type}</option>)}</select></Panel><PlanEditor plan={plan} onSaved={saved=>{setPlan(saved);void refreshPlans();}}/><Panel title="Evidence / permission" kicker="SERVER-OWNED">{coach?<div className="space-y-2 text-xs"><div>CONTEXT: 4H {coach.market_context.regime_4h??"—"} · 1H {coach.market_context.context_1h??"—"}</div><div>Data: {coach.market_context.status} · Playbook: {coach.market_context.playbook_state??"UNAVAILABLE"}</div><div>LOCATION: {coach.market_context.location} · {coach.execution.condition_evidence.includes("LOCATION_VALID")?"YES":"NO"}</div><div>REACTION: {coach.market_context.reaction??"MISSING"} · {coach.market_context.reaction_status}</div><div>TRIGGER: unavailable</div><div>RISK: {coach.risk.policy_status} · {coach.risk.correlation_group} · ${coach.risk.known_group_risk_usdt}</div><div>PERMISSION: {permission}</div>{coach.execution.next_conditions.map(x=><div key={x}>Next: {x}</div>)}{coach.execution.blocking_reasons.map(x=><div key={x} className="text-amber-300">Block: {x}</div>)}{coach.execution.warnings.map(x=><div key={x} className="text-rose-300">Warning: {x}</div>)}</div>:<p className="text-xs text-slate-500">No active position coach is available.</p>}</Panel><Panel title="Sizing" kicker="BACKEND CALCULATION">{plan&&<button onClick={()=>void size()} className="text-xs text-cyan-300">Refresh sizing</button>}{sizing&&<div className="mt-2 space-y-1 text-xs">Risk ${sizing.risk_budget_usdt} · used ${sizing.group_risk_used_usdt} · remaining ${sizing.group_risk_remaining_usdt}<br/>Max qty {sizing.maximum_quantity} · notional ${sizing.maximum_notional}<br/>Minimum leverage for margin fit: {sizing.minimum_leverage_for_margin_fit??"—"}<br/>Estimated margin: {sizing.estimated_margin_usdt??"—"} · LIQUIDATION DATA {sizing.liquidation_status}{sizing.stages.map(s=><div key={s.state} className="mt-2 border-t border-white/10 pt-2">{s.state} · {s.status} · allocation {s.allocation??"—"}<br/>risk ${s.stage_risk_usdt??"—"} · qty {s.quantity??"—"} · notional ${s.notional??"—"}<br/>{s.reasons.join("; ")}</div>)}</div>}</Panel></aside></div></>}
