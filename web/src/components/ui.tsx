import type { ReactNode } from "react";
export function PageHeader({ eyebrow, title, detail, action }: { eyebrow: string; title: string; detail: string; action?: ReactNode }) { return <div className="mb-6 flex flex-col justify-between gap-3 md:flex-row md:items-end"><div><div className="mb-2 font-mono text-[10px] tracking-[.18em] text-cyan-400">{eyebrow}</div><h1 className="text-2xl font-semibold tracking-tight text-white">{title}</h1><p className="mt-1 max-w-2xl text-sm text-slate-500">{detail}</p></div>{action}</div> }
export function Panel({ title, kicker, children, className="" }: { title?: string; kicker?: string; children: ReactNode; className?: string }) { return <section className={`rounded-md border border-white/8 bg-[#0d121a] ${className}`}>{(title||kicker)&&<div className="flex items-center justify-between border-b border-white/8 px-4 py-3"><h2 className="text-sm font-medium text-slate-200">{title}</h2>{kicker&&<span className="font-mono text-[9px] tracking-[.14em] text-slate-600">{kicker}</span>}</div>}<div className="p-4">{children}</div></section> }
export function Metric({ label, value, note, tone="" }: { label: string; value: ReactNode; note?: string; tone?: string }) { return <div className="rounded-md border border-white/8 bg-[#0d121a] p-4"><div className="font-mono text-[9px] tracking-[.16em] text-slate-500">{label}</div><div className={`mt-2 text-xl font-semibold tabular-nums ${tone}`}>{value}</div>{note&&<div className="mt-1 text-xs text-slate-600">{note}</div>}</div> }
export function EmptyState({ title, detail }: { title: string; detail: string }) { return <div className="grid min-h-36 place-items-center rounded border border-dashed border-white/10 bg-white/[.015] p-8 text-center"><div><div className="text-sm text-slate-300">{title}</div><div className="mt-1 text-xs text-slate-600">{detail}</div></div></div> }
export function ErrorState({ message }: { message: string }) { return <div className="rounded border border-red-500/20 bg-red-500/5 p-4 text-sm text-red-300"><div className="font-medium">API unavailable</div><div className="mt-1 text-xs text-red-300/60">{message}</div></div> }
